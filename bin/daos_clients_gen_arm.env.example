# Environment file for the daos_clients_gen_arm.sh script
# Copy this file to bin/daos_clients_gen_arm.env
# Then run bin/daos_clients_gen_arm.sh
# When daos_clients_gen_arm.sh detects a daos_clients_gen_arm.env file
# in the same directory it will source that file and use the variable
# values from that file instead of the defaults.

# This file only would need to be created if you are running
# daos_clients_gen_arm.sh directly. The daos_clients_deploy.sh file
# will call daos_clients_gen_arm.sh to create an
# arm/daos/azuredeploy_client.json file.

# If you only want to create the arm/daos/azuredeploy_client.json file
# and not actually create a deployment you can copy this template
# to bin/daos_clients_gen_arm.env and then set the variable values.
# You can then run daos_clients_gen_arm.sh to create an
# arm/daos/azuredeploy_client.json file.

export DAOS_AZ_RG_NAME="$(az config get defaults.group -o tsv --only-show-errors | awk '{print $3}')"
export DAOS_AZ_RESOURCE_PREFIX=""
export DAOS_AZ_ARM_SRC_TEMPLATE="azuredeploy_client_template.json"
export DAOS_AZ_ARM_DEST_TEMPLATE="azuredeploy_client.json"
export DAOS_VM_FILES_DIR="vm_files/daos_client"
export DAOS_VM_ENTRY_SCRIPT="daos_client_setup.sh"
export DAOS_VM_BASE_NAME="daos-client"
export DAOS_AZ_clientCount="1"
