# Environment file for the daos_servers_gen_arm.sh script
# Copy this file to bin/daos_servers_gen_arm.env
# Then run bin/daos_servers_gen_arm.sh
# When daos_servers_gen_arm.sh detects a daos_servers_gen_arm.env file
# in the same directory it will source that file and use the variable
# values from that file instead of the defaults.

# This file only would need to be created if you are running
# daos_servers_gen_arm.sh directly. The daos_servers_deploy.sh file
# will call daos_servers_gen_arm.sh to create an
# arm/daos/azuredeploy_server.json file.

# If you only want to create the arm/daos/azuredeploy_server.json file
# and not actually create a deployment you can copy this template
# to bin/daos_servers_gen_arm.env and then set the variable values.
# You can then run daos_servers_gen_arm.sh to create an
# arm/daos/azuredeploy_server.json file.

export DAOS_AZ_RG_NAME="$(az config get defaults.group -o tsv --only-show-errors | awk '{print $3}')"
export DAOS_AZ_RESOURCE_PREFIX=""
export DAOS_AZ_ARM_SRC_TEMPLATE="azuredeploy_server_template.json"
export DAOS_AZ_ARM_DEST_TEMPLATE="azuredeploy_server.json"
export DAOS_VM_FILES_DIR="vm_files/daos_server"
export DAOS_VM_ENTRY_SCRIPT="daos_server_setup.sh"
export DAOS_VM_BASE_NAME="daos-server"
export DAOS_AZ_serverCount="3"
